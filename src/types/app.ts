export type UserToken = {
    token: string
}
export type loginInfo = {
    username: string,
    password: string
}
export type UserInfo = {
    id: number;
    name: string;
    email: string;
    password: string;
    username:string
}
